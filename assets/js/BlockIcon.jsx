import { hot } from 'react-hot-loader/root';
import React from 'react';
import { Icon } from '@wordpress/components';

const BlockIcon = () => <Icon icon="admin-plugins" />;

export default hot(BlockIcon);
